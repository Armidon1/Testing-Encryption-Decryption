[tool.poetry]
name = "encryption-performance-plots"
version = "0.1.0"
description = "A project to analyze and visualize the performance of various encryption algorithms."
authors = ["Your Name <your.email@example.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"
matplotlib = "^3.4"
seaborn = "^0.11"
pandas = "^1.3"
cryptography = "^3.4"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"